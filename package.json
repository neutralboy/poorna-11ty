{
  "name": "poorna-11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "eleventy && tailwindcss -i ./_includes/input.css -o ./_site/index.css --minify && node scripts/version-css.js && cp ./_redirects ./_site/_redirects && cp ./_headers ./_site/_headers",
    "dev": "eleventy --serve --watch",
    "css": "tailwindcss -i ./_includes/input.css -o ./_site/index.css --watch --minify"
  },
  "author": "neutralboy",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "html-minifier-terser": "^7.2.0"
  },
  "devDependencies": {
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "@tailwindcss/cli": "^4.3.3",
    "@tailwindcss/typography": "^0.5.20",
    "tailwindcss": "^4.3.3"
  }
}
