{
  "name": "poorna-11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "eleventy && tailwindcss -i ./_includes/input.css -o ./_site/index.css --minify",
    "dev": "eleventy --serve --watch",
    "css": "tailwindcss -i ./_includes/input.css -o ./_site/index.css --watch --minify"
  },
  "author": "neutralboy",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "html-minifier": "^4.0.0"
  },
  "devDependencies": {
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.2.7"
  }
}
